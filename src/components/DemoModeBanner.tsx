/**
 * Demo Mode Banner
 * Displays a notice when static data is being used (production demo mode)
 */

'use client';

import { useEffect, useState } from 'react';
import { Sparkles } from 'lucide-react';

/**
 * Demo Mode Banner - DISABLED
 * 
 * System is now live on BSC Testnet with real blockchain data only.
 * No static/demo data is used. All markets are from the blockchain.
 */

export function DemoModeBanner() {
  // Demo mode disabled - system is live
  return null;
}
