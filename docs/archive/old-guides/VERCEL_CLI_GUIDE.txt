━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 VERCEL CLI DEPLOYMENT - STEP BY STEP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

You're logged in as: nepalz ✅

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 1: Get WalletConnect Project ID (REQUIRED - 2 minutes)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Open in browser: https://cloud.walletconnect.com/

2. Sign up or Login (free account)

3. Click "Create New Project"

4. Give it a name (e.g., "PredictBNB")

5. Copy your Project ID (looks like: abc123def456...)

6. Save it - you'll need it in the next step!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 2: Deploy with Simple Command
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Option A: Use the Interactive Script (EASIEST)
──────────────────────────────────────────────

Run this command and follow the prompts:

    ./deploy-vercel.sh

The script will ask you for:
- WalletConnect Project ID (from Step 1)
- Optional: AI API keys (can skip and add later)

Option B: Manual One-Line Deploy
─────────────────────────────────

Replace YOUR_WALLETCONNECT_ID with your actual ID:

vercel --prod \
  -e NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=YOUR_WALLETCONNECT_ID \
  -e NEXT_PUBLIC_PREDICTION_MARKET_ADDRESS=0x5FbDB2315678afecb367f032d93F642f64180aa3 \
  -e NEXT_PUBLIC_AI_ORACLE_ADDRESS=0xe7f1725E7734CE288F8367e1Bb143E90bb3F0512 \
  -e NEXT_PUBLIC_GASLESS_RELAYER_ADDRESS=0x9fE46736679d2D9a65F0992F2272dE9f3c7fa6e0 \
  -e NEXT_PUBLIC_DEFAULT_CHAIN_ID=97 \
  -e NEXT_PUBLIC_ENABLE_TESTNET=true \
  -e NEXT_PUBLIC_SITE_NAME=PredictBNB

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 3: Wait for Build (2-3 minutes)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Vercel will:
1. Upload your code
2. Install dependencies
3. Build Next.js app
4. Deploy to production

You'll see a URL when it's done!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
IMPORTANT: These are LOCAL contract addresses!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The addresses above only work with your local Hardhat node.

For a working demo on Vercel, you need to:

1. Deploy contracts to BSC Testnet:
   
   cd contracts
   npx hardhat run scripts/deploy-bsc-testnet.js --network bscTestnet

2. Update Vercel with new addresses:

   vercel env rm NEXT_PUBLIC_PREDICTION_MARKET_ADDRESS production
   vercel env add NEXT_PUBLIC_PREDICTION_MARKET_ADDRESS production
   (Enter your new address)

   # Repeat for AI_ORACLE and GASLESS_RELAYER

3. Redeploy:
   
   vercel --prod

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Quick Reference Commands
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Check who you're logged in as
vercel whoami

# List your projects
vercel ls

# View environment variables
vercel env ls

# Add an environment variable
vercel env add NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID production

# Remove an environment variable
vercel env rm NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID production

# Redeploy
vercel --prod

# View deployment logs
vercel logs

# Link to existing project (if needed)
vercel link

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Ready to deploy? Run this:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    ./deploy-vercel.sh

OR get your WalletConnect ID first and use the one-line command above!

Good luck! 🚀
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
